const { globalPrefix: prefix } = require('../../utils/globalConfig');

const tColors = {
  color: {
    theme: {
      primary: { value: '#db3236' },
    },
  },
};

module.exports = {
  [prefix]: tColors,
};
