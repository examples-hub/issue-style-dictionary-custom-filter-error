const { globalPrefix: prefix } = require('../../utils/globalConfig');

const tColors = {
  color: {
    theme: {
      primary: { value: '#db3236' },
      secondary: { value: '#fcc20d' },
    },
  },
};

module.exports = {
  [prefix]: tColors,
};
