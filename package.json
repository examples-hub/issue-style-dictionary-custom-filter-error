{
  "name": "@pgd/design-tokens",
  "version": "1.0.0",
  "main": "src/index.js",
  "module": "src/index.js",
  "scripts": {
    "build": "rimraf dist && npm run build:tokens",
    "build:tokens": "node ./build.js",
    "dev": "rimraf dist && npm run build:tokens",
    "start": "",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:log": "node index.js --name nested-a"
  },
  "devDependencies": {
    "fs-extra": "^9.1.0",
    "rimraf": "3.0.2",
    "style-dictionary": "^3.0.0-rc.5"
  }
}
